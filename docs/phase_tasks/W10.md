# Task W10

MEMORY OBSERVABILITY AND PRIVACY CONTROLS. Keep this small; do NOT build an
admin UI.

OWNED FILES:
  src/research_radar/bot/commands/memory.py
  scripts/inspect_user_memory.py
  tests/bot/test_memory_commands.py

Follow the established pattern of the existing command modules in
src/research_radar/bot/commands/ (read ask.py and ingest.py first — match their
registration function shape, their Protocol-based service typing, their embed
usage via research_radar.bot.embeds, and their error handling). Do NOT edit
client.py; expose a `register_memory_commands(tree, memory_service, settings)`
function that the coordinator will wire in.

Implement two slash commands:
  /memory-status          -> backend name, enabled, healthy, persistence path,
                             episode count if available
  /memory-search <query>  -> up to settings.user_memory_max_results facts

A /memory-forget command is IN SCOPE ONLY IF the W1 spike report
(docs/spikes/graphiti_compat.md in your worktree) confirms a safe, targeted
deletion API in graphiti-core 0.29.x. If it does not, do not implement it; say
so explicitly in worker_done and add a one-line note in the module docstring.
Do not implement a "delete everything" command.

Privacy requirements, which are the real point of this task:
- When settings.discord_owner_user_id is configured, BOTH commands are
  restricted to that user. A non-owner gets a short ephemeral refusal and the
  backend is never queried. Test this.
- Output renders only the fact text, its memory class, and its temporal
  validity. Never dump internal graph metadata: no node/edge UUIDs, no
  embeddings, no raw driver payloads, no group ids, no file paths beyond the
  single persistence path in /memory-status.
- Truncate each fact for Discord and cap the total message length.
- Backend unavailable is a normal outcome, not an error: report "personal
  memory is unavailable" plainly. No stack trace, no exception text reaches
  Discord.
- Assert in a test that a credential-shaped string seeded into the fake store's
  fact text is not what these commands are for — and more importantly that no
  command output includes anything outside the whitelisted fields.

scripts/inspect_user_memory.py is a small local CLI: print MemoryStatus, and
with a `--query` argument print matching facts. It must work with
USER_MEMORY_BACKEND=disabled (printing that memory is disabled and exiting 0)
and must never print a credential or a raw driver object. Follow the structure
of the existing scripts/inspect_research_store.py.

Tests use research_radar.memory.fakes.FakeUserMemoryStore (owned by W2) and the
existing test patterns for Discord interactions in tests/bot/. Cover: owner
restriction, disabled backend, outage, field whitelisting, and truncation.

--- WORKING AGREEMENT (identical for every worker in this phase) ---
Repo: ResearchRadar, Python 3.12, package root src/research_radar.
You are ONE of twelve parallel workers. Other workers are editing other files
in other worktrees at the same time.

READ FIRST, IN FULL: docs/PHASE_CONTRACTS.md in your worktree. It is the
binding interface contract for this phase. Implement exactly the module paths,
class names, method signatures, field names, and settings names it defines. Do
not rename or "improve" a contract name. If a contract is genuinely wrong,
escalate with `orca orchestration ask` instead of changing it unilaterally.

Your worktree is already on your own branch, based on
feat/personal-research-chat-memory.

Run tests and lint with the SHARED interpreter. Do NOT create a virtualenv and
do NOT pip install anything unless your task explicitly says so:
  cd "$PWD"   # your worktree root
  PYTHONPATH="$PWD/src" /Users/alvinluong/ResearchRadar/.venv/bin/python -m pytest -q
  /Users/alvinluong/ResearchRadar/.venv/bin/ruff check .

House style: ruff rules E,F,I,B,UP; line length 100; `from __future__ import
annotations` first in every module; docstrings on public classes and methods;
pytest asyncio_mode=auto (do NOT add @pytest.mark.asyncio).

Hard rules:
- Edit ONLY the files listed under OWNED FILES. Touching another worker's file
  causes an integration conflict and will be reverted.
- Never write a real credential into any file, test, fixture, log, or commit.
- Never push to main, never merge, never open a PR. Commit on your own branch.
- Tests must pass offline with no network access.
- Do not run blocking CPU/IO work directly on an asyncio event loop; wrap
  SQLite / filesystem / embedding / Pinecone calls with asyncio.to_thread.
- When finished, commit your work, then send exactly one worker_done with
  --outcome succeeded or --outcome failed and a 3-sentence summary.
