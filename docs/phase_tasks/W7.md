# Task W7

DISCORD MENTION BOUNDARY. See PHASE_CONTRACTS section 11.

OWNED FILES:
  src/research_radar/bot/mention.py
  src/research_radar/bot/client.py         (edit; you are the sole owner)
  tests/bot/test_mention_policy.py
  tests/bot/test_mention_handler.py
(create tests/bot/__init__.py only if tests/bot/ has no __init__.py already)

Implement MentionAdmission and MentionPolicy exactly as contracted, plus the
on_message handler wiring in client.py.

Intents: change _application_intents() to request guilds, guild_messages, and
dm_messages (dm_messages only when settings.discord_dm_chat). Do NOT enable
message_content. Add a module docstring comment recording WHY: Discord delivers
message content without the privileged Message Content intent for messages that
mention the app and for DMs to the app, which is exactly this feature's
surface. Cite that this is the documented mention/DM exception.

_application_intents must take the settings (or the relevant flags) so the DM
intent is conditional. Preserve backward compatibility for its existing
callers, or update them — client.py is yours.

MentionPolicy.admit rejection order, first match wins:
  self_message        message.author.id == bot_user_id
  bot_author          message.author.bot
  dm_disabled         DM and not settings.discord_dm_chat
  mention_disabled    guild message and not settings.discord_chat_on_mention
  no_mention          guild message where the bot is not explicitly mentioned
  channel_not_allowed guild message, settings.discord_allowed_channel_ids
                      non-empty, and channel id not in it
  owner_only          settings.discord_owner_user_id set and author id differs
DMs bypass the mention requirement but still respect owner_only.

"Explicitly mentioned" means the bot user id appears in message.mentions (or
the raw content contains <@id>/<@!id>). @everyone, @here, and a role mention
that merely includes the bot must NOT count. Test that.

Text stripping removes every <@id> and <@!id> form for the bot, collapses
whitespace, and trims. Accepted-but-empty text is accepted with text="".

ResearchRadarBot gains an optional chat_service constructor keyword typed
against a local Protocol (do NOT import research_radar.chat.service — that
module is owned by another worker and may not exist in your worktree; define
`class ChatServiceProtocol(Protocol): async def chat(self, request) -> object:
...` locally, mirroring PHASE_CONTRACTS section 10). Register on_message only
when chat_service is not None.

on_message must contain NO research logic:
  1. admission = self._mention_policy.admit(message, bot_user_id=self.user.id)
  2. if not admission.accepted: return immediately (no logging of content)
  3. if admission.text == "": reply with a short fixed usage hint, return.
     Make ZERO backend calls on this path.
  4. build ChatRequest-shaped input, `async with message.channel.typing():`
     await chat_service.chat(request)
  5. chunk response.text at <= 1900 chars on paragraph/newline boundaries where
     possible, send each chunk in order
  6. on exception: log sanitized (exception type and a fixed message only,
     never the user's text or the exception's str if it could echo input) and
     reply with one short safe error message.
Reply exactly once per accepted message.

Tests use lightweight fake message/author/channel/guild objects, not a live
gateway. Cover every rejection reason, the empty-mention hint, chunking of a
>1900 char response, exactly-once reply, role/@everyone mentions not counting,
and that a rejected message triggers zero chat_service calls.

--- WORKING AGREEMENT (identical for every worker in this phase) ---
Repo: ResearchRadar, Python 3.12, package root src/research_radar.
You are ONE of twelve parallel workers. Other workers are editing other files
in other worktrees at the same time.

READ FIRST, IN FULL: docs/PHASE_CONTRACTS.md in your worktree. It is the
binding interface contract for this phase. Implement exactly the module paths,
class names, method signatures, field names, and settings names it defines. Do
not rename or "improve" a contract name. If a contract is genuinely wrong,
escalate with `orca orchestration ask` instead of changing it unilaterally.

Your worktree is already on your own branch, based on
feat/personal-research-chat-memory.

Run tests and lint with the SHARED interpreter. Do NOT create a virtualenv and
do NOT pip install anything unless your task explicitly says so:
  cd "$PWD"   # your worktree root
  PYTHONPATH="$PWD/src" /Users/alvinluong/ResearchRadar/.venv/bin/python -m pytest -q
  /Users/alvinluong/ResearchRadar/.venv/bin/ruff check .

House style: ruff rules E,F,I,B,UP; line length 100; `from __future__ import
annotations` first in every module; docstrings on public classes and methods;
pytest asyncio_mode=auto (do NOT add @pytest.mark.asyncio).

Hard rules:
- Edit ONLY the files listed under OWNED FILES. Touching another worker's file
  causes an integration conflict and will be reverted.
- Never write a real credential into any file, test, fixture, log, or commit.
- Never push to main, never merge, never open a PR. Commit on your own branch.
- Tests must pass offline with no network access.
- Do not run blocking CPU/IO work directly on an asyncio event loop; wrap
  SQLite / filesystem / embedding / Pinecone calls with asyncio.to_thread.
- When finished, commit your work, then send exactly one worker_done with
  --outcome succeeded or --outcome failed and a 3-sentence summary.
