# Task W5

CHAT ROUTING. See PHASE_CONTRACTS sections 6 and 7.

OWNED FILES:
  src/research_radar/chat/__init__.py
  src/research_radar/chat/models.py
  src/research_radar/chat/router.py
  tests/chat/__init__.py
  tests/chat/test_chat_models.py
  tests/chat/test_router.py

Implement ChatMode, EvidenceScope, ChatRequest, ChatResponse, RouteDecision,
and ChatRouter exactly as contracted.

__init__.py re-exports ChatMode, EvidenceScope, ChatRequest, ChatResponse,
RouteDecision, ChatRouter. Other workers will add ChatService, EvidencePacket,
and build_chat_prompt to the same package later — write the __init__ so those
additions are a clean append (import them lazily or leave a clearly marked
section), and do NOT import chat.service, chat.evidence, or chat.prompt
yourself; those modules do not exist in your worktree.

ChatRouter.route is async and must NEVER raise. Deterministic rules run first
and are authoritative for the clear cases listed in the contract. The optional
LLM assist:
- Only runs when the deterministic pass is genuinely ambiguous (no rule fired
  with confidence). Define and document that condition.
- Uses llm_provider.generate_structured with a small Pydantic response model
  you define in router.py.
- Any exception, timeout, or invalid response falls back to the deterministic
  decision silently (log at debug, sanitized).
- MUST NOT run for greetings/small talk. Assert this in a test: route a
  greeting with a provider whose generate_structured raises if called, and
  confirm no call happened and mode is CONVERSATIONAL.

search_query normalization: strip the leading imperative ("find", "search for",
"look up", "show me", "what is the recent work on", "any papers about"), drop
trailing punctuation, collapse whitespace, and keep the substantive topic
phrase. Empty for non-research modes.

RESEARCH_LIVE is never returned by the router. The router only grants
allows_live_discovery; ChatService decides.

Tests must cover every example from the phase brief:
  "hello"                                          -> CONVERSATIONAL, no research
  "what research areas do I seem to care about?"   -> PERSONAL_MEMORY, no live
  "find recent work about medical VLA reasoning"   -> RESEARCH_STORED + live ok
  "compare world models and VLA for medical AI"    -> RESEARCH_STORED + live ok
  "I don't want to pursue GAN-based directions"    -> CONVERSATIONAL,
                                                      needs_user_memory=True
plus project_hint -> PROJECT_RESEARCH, and empty/whitespace text handling.

--- WORKING AGREEMENT (identical for every worker in this phase) ---
Repo: ResearchRadar, Python 3.12, package root src/research_radar.
You are ONE of twelve parallel workers. Other workers are editing other files
in other worktrees at the same time.

READ FIRST, IN FULL: docs/PHASE_CONTRACTS.md in your worktree. It is the
binding interface contract for this phase. Implement exactly the module paths,
class names, method signatures, field names, and settings names it defines. Do
not rename or "improve" a contract name. If a contract is genuinely wrong,
escalate with `orca orchestration ask` instead of changing it unilaterally.

Your worktree is already on your own branch, based on
feat/personal-research-chat-memory.

Run tests and lint with the SHARED interpreter. Do NOT create a virtualenv and
do NOT pip install anything unless your task explicitly says so:
  cd "$PWD"   # your worktree root
  PYTHONPATH="$PWD/src" /Users/alvinluong/ResearchRadar/.venv/bin/python -m pytest -q
  /Users/alvinluong/ResearchRadar/.venv/bin/ruff check .

House style: ruff rules E,F,I,B,UP; line length 100; `from __future__ import
annotations` first in every module; docstrings on public classes and methods;
pytest asyncio_mode=auto (do NOT add @pytest.mark.asyncio).

Hard rules:
- Edit ONLY the files listed under OWNED FILES. Touching another worker's file
  causes an integration conflict and will be reverted.
- Never write a real credential into any file, test, fixture, log, or commit.
- Never push to main, never merge, never open a PR. Commit on your own branch.
- Tests must pass offline with no network access.
- Do not run blocking CPU/IO work directly on an asyncio event loop; wrap
  SQLite / filesystem / embedding / Pinecone calls with asyncio.to_thread.
- When finished, commit your work, then send exactly one worker_done with
  --outcome succeeded or --outcome failed and a 3-sentence summary.
