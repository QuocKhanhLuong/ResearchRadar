# Task W1

GRAPHITI COMPATIBILITY SPIKE. Investigation + spike only; no production edits.

OWNED FILES:
  docs/spikes/graphiti_compat.md   (create)
  scripts/spike_graphiti.py        (create)

You MAY pip install into the shared venv for this task only:
  /Users/alvinluong/ResearchRadar/.venv/bin/pip install 'graphiti-core>=0.29' 'kuzu>=0.11'

Coordinator has already established (verify, do not re-derive from scratch):
- graphiti-core latest is 0.29.3; kuzu latest is 0.11.3.
- Embedded local backend is graphiti_core.driver.kuzu_driver.KuzuDriver(db=<path>).
- Kuzu is DEPRECATED upstream (emits DeprecationWarning: "the upstream Kuzu
  project is no longer maintained. Migrate to Neo4j or FalkorDB"). It is still
  present and functional, and it is the only embedded/zero-server option, so
  this phase keeps it. Neo4j/FalkorDB are out of scope.

Deliver in docs/spikes/graphiti_compat.md:
1. Exact installed versions of graphiti-core, kuzu, and their transitive deps
   that matter (pydantic version compatibility with this repo's pydantic>=2.7 —
   this is the single highest-risk item, check it first and report conflicts).
2. The EXACT import paths and constructor/method signatures for: Graphiti,
   KuzuDriver, build_indices_and_constraints, add_episode (all parameters,
   including EpisodeType and group_id), search, and the close/shutdown call.
   Copy real signatures out of the installed package; do not recall them.
3. How to supply an OpenAI-COMPATIBLE custom base_url LLM client and embedder
   (this repo talks to GoRouter via an OpenAI-compatible chat-completions API,
   see src/research_radar/reader/llm/remote.py). Report the exact classes and
   config objects, e.g. graphiti_core.llm_client.* and graphiti_core.embedder.*.
4. Whether custom entity/edge types are supported in 0.29.x and the exact
   parameter name used to pass them to add_episode/search.
5. Temporal validity fields available on returned edges (valid_at, invalid_at,
   fact, created_at) and how to read them.
6. Whether Kuzu persistence survives process restart with a file path, and the
   exact on-disk layout produced at that path.
7. A dependency proposal for pyproject.toml optional extra `memory`, with exact
   version pins, and any conflict with the existing dependency set.
8. A short "deviation record" section: Kuzu deprecation, why we keep it, and
   the migration escape hatch.

scripts/spike_graphiti.py must be a small standalone script that constructs a
KuzuDriver against a temp path, builds indices, and prints the resolved
signatures/versions it discovered. It must NOT require network or an API key to
run its introspection path; guard any call that needs an LLM behind an explicit
flag and skip it by default. It is a spike, not production code, and it is not
imported by the package.

Report your findings in the worker_done body in enough detail that another
worker can implement the adapter from it. If pydantic or any other dependency
conflicts with this repo, say so loudly and explicitly.

--- WORKING AGREEMENT (identical for every worker in this phase) ---
Repo: ResearchRadar, Python 3.12, package root src/research_radar.
You are ONE of twelve parallel workers. Other workers are editing other files
in other worktrees at the same time.

READ FIRST, IN FULL: docs/PHASE_CONTRACTS.md in your worktree. It is the
binding interface contract for this phase. Implement exactly the module paths,
class names, method signatures, field names, and settings names it defines. Do
not rename or "improve" a contract name. If a contract is genuinely wrong,
escalate with `orca orchestration ask` instead of changing it unilaterally.

Your worktree is already on your own branch, based on
feat/personal-research-chat-memory.

Run tests and lint with the SHARED interpreter. Do NOT create a virtualenv and
do NOT pip install anything unless your task explicitly says so:
  cd "$PWD"   # your worktree root
  PYTHONPATH="$PWD/src" /Users/alvinluong/ResearchRadar/.venv/bin/python -m pytest -q
  /Users/alvinluong/ResearchRadar/.venv/bin/ruff check .

House style: ruff rules E,F,I,B,UP; line length 100; `from __future__ import
annotations` first in every module; docstrings on public classes and methods;
pytest asyncio_mode=auto (do NOT add @pytest.mark.asyncio).

Hard rules:
- Edit ONLY the files listed under OWNED FILES. Touching another worker's file
  causes an integration conflict and will be reverted.
- Never write a real credential into any file, test, fixture, log, or commit.
- Never push to main, never merge, never open a PR. Commit on your own branch.
- Tests must pass offline with no network access.
- Do not run blocking CPU/IO work directly on an asyncio event loop; wrap
  SQLite / filesystem / embedding / Pinecone calls with asyncio.to_thread.
- When finished, commit your work, then send exactly one worker_done with
  --outcome succeeded or --outcome failed and a 3-sentence summary.
