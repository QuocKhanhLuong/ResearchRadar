# Task W9

ENVIRONMENT + CONFIGURATION. See PHASE_CONTRACTS section 12. You are the SOLE
owner of config.py, .env.example, and docs/ENVIRONMENT.md for this phase.

OWNED FILES:
  src/research_radar/config.py     (edit)
  .env.example                     (rewrite)
  docs/ENVIRONMENT.md              (create)
  tests/test_config_chat_memory.py

Add exactly the settings listed in PHASE_CONTRACTS section 12, keeping the
existing settings and validators intact.

discord_allowed_channel_ids parses a CSV env value (DISCORD_ALLOWED_CHANNEL_IDS)
into a tuple[int, ...]. Empty/absent -> (). Whitespace tolerated. A
non-integer entry raises a clear ConfigurationError-style validation message
naming the variable. Use a pydantic field_validator with mode="before".

user_memory_backend gets a validator accepting only {"disabled","graphiti"}
(case-insensitive, stripped), mirroring the existing embedding_provider
validator's style and error message shape.

Add a helper `user_memory_db_path_resolved(self) -> Path` mirroring
artifact_root_path(): expanduser().resolve(), no directory creation.

Rewrite .env.example as a COMPLETE, safe-to-commit template covering EVERY
variable the app reads, grouped with banner comments in this order: Discord;
Storage; Scholarly providers; LLM / GoRouter; Embeddings; Pinecone; Personal
memory; Research ingestion; Scheduler. No real values, ever. Each group gets a
one-line comment saying whether it is required, and for which feature. Include
HTTP_TIMEOUT_SECONDS (keep the code default and use that same number in the
template).

> **Superseded.** This paragraph originally read "the current default in
> config.py is 20.0". The default was raised to 60.0 at operator request during
> the final review pass; `config.py`, `providers/base.py`, `.env.example`,
> `README.md`, and `tests/test_config.py` all carry 60. The instruction itself —
> code default and template must agree — still holds.

docs/ENVIRONMENT.md is a compact matrix with these columns:
  Feature | Required envs | Optional envs | Offline / unset behaviour
Cover at least: Discord bot launch, Discord mention chat, DM chat, SQLite
storage, artifact cache, OpenAlex, Semantic Scholar, arXiv, LLM synthesis,
local embeddings, Pinecone semantic index, personal memory (Graphiti/Kuzu),
bounded live discovery, watch scheduler, digest scheduler. Add a short section
listing which Discord Developer Portal toggles the user must enable — and
state explicitly that the privileged MESSAGE CONTENT intent is NOT required,
because Discord delivers content for messages that mention the app and for DMs
to the app. Add a section documenting that USER_MEMORY_BACKEND=graphiti
requires `pip install -e ".[memory]"` and that leaving it disabled keeps chat
fully functional.

Tests must prove: every new setting has a working default; empty string env
values do not crash construction (env_ignore_empty is already on); the CSV
parser handles "", " 1, 2 ,3 ", and rejects "abc"; the backend validator
rejects "neo4j"; user_memory_db_path_resolved returns an absolute path and
creates nothing. Use monkeypatch.setenv and construct Settings directly with
_env_file=None so the developer's real .env cannot leak into the test.

--- WORKING AGREEMENT (identical for every worker in this phase) ---
Repo: ResearchRadar, Python 3.12, package root src/research_radar.
You are ONE of twelve parallel workers. Other workers are editing other files
in other worktrees at the same time.

READ FIRST, IN FULL: docs/PHASE_CONTRACTS.md in your worktree. It is the
binding interface contract for this phase. Implement exactly the module paths,
class names, method signatures, field names, and settings names it defines. Do
not rename or "improve" a contract name. If a contract is genuinely wrong,
escalate with `orca orchestration ask` instead of changing it unilaterally.

Your worktree is already on your own branch, based on
feat/personal-research-chat-memory.

Run tests and lint with the SHARED interpreter. Do NOT create a virtualenv and
do NOT pip install anything unless your task explicitly says so:
  cd "$PWD"   # your worktree root
  PYTHONPATH="$PWD/src" /Users/alvinluong/ResearchRadar/.venv/bin/python -m pytest -q
  /Users/alvinluong/ResearchRadar/.venv/bin/ruff check .

House style: ruff rules E,F,I,B,UP; line length 100; `from __future__ import
annotations` first in every module; docstrings on public classes and methods;
pytest asyncio_mode=auto (do NOT add @pytest.mark.asyncio).

Hard rules:
- Edit ONLY the files listed under OWNED FILES. Touching another worker's file
  causes an integration conflict and will be reverted.
- Never write a real credential into any file, test, fixture, log, or commit.
- Never push to main, never merge, never open a PR. Commit on your own branch.
- Tests must pass offline with no network access.
- Do not run blocking CPU/IO work directly on an asyncio event loop; wrap
  SQLite / filesystem / embedding / Pinecone calls with asyncio.to_thread.
- When finished, commit your work, then send exactly one worker_done with
  --outcome succeeded or --outcome failed and a 3-sentence summary.
