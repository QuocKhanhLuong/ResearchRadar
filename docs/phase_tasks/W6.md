# Task W6

GENERAL RESEARCH CHAT SERVICE. See PHASE_CONTRACTS sections 8 and 10. This is
the largest workstream; read the contract twice before writing code.

OWNED FILES:
  src/research_radar/chat/evidence.py
  src/research_radar/chat/service.py
  tests/chat/test_evidence.py
  tests/chat/test_chat_service.py

You import, and must NOT edit: research_radar.chat.models, .router (W5),
.prompt (W8), research_radar.memory.* (W2/W3/W4), and the existing
research_radar.research.*, research_radar.storage.*, research_radar.semantic.*
modules. If chat/prompt.py is absent in your worktree, import it defensively at
call time and fall back to a minimal local prompt builder marked with a clear
TODO comment naming W8 as the owner — the coordinator will remove the fallback
at integration.

REUSE, do not reimplement:
- src/research_radar/research/ingestion.py IngestionService.ingest_research_topic
  is the ONLY live-discovery path. Do not add a second provider stack.
- src/research_radar/research/hybrid.py HybridRetriever for stored retrieval.
  Study how src/research_radar/research/ask.py uses it and follow that pattern,
  including its SQLite resolution guarantees.
- The existing ResearchRepository for all SQLite reads.

Implement evidence.py: StoredEvidenceItem, DiscoveryEvidenceItem, ProjectMemory,
EvidencePacket, plus a citation validator
`validate_cited_ids(text_ids, packet) -> tuple[str, ...]` that drops any id not
in the packet's allowed sets. EvidencePacket.evidence_scope derives from what is
present: nothing -> NONE; only user memory -> USER_MEMORY; stored items that all
have PaperCards -> STORED_CARDS; stored items without cards -> STORED_METADATA;
only discovery items -> DISCOVERY_METADATA; a mix -> MIXED.

Implement ChatBudget and ChatService exactly as contracted, with the ten-step
pipeline in the contracted order.

Non-negotiable behaviours:
- auto_read is ALWAYS 0 on the ingestion call. Mention chat performs zero
  automatic full-PDF reads. Assert this in a test.
- The discovery limit is min(budget.max_discovery_results,
  settings-supplied limit, 12). Clamp defensively inside the service; never
  trust the caller.
- At most ONE ingestion call per chat turn, even if stored evidence is still
  thin afterwards. Assert the call count.
- Every paper id in the response was read back from SQLite after ingestion. A
  provider id or a vector-index id must never appear in ChatResponse.paper_ids.
- No project and no watch topic is required for a research query.
- LLM failure: return a concise safe error string, degraded=True, no invented
  content, and if live discovery persisted papers, include those ids and say
  they were stored. Critically: DO NOT write user memory on this path.
- Memory outage, semantic outage, and ingestion failure each degrade with a
  sanitized log line and the chat still returns.
- Wrap every synchronous repository/filesystem/embedding/index call reachable
  from chat() in asyncio.to_thread. Nothing blocking runs on the caller's loop.
- Capture runs LAST, after a successful response, and only via
  MemoryCapturePolicy. The service never decides on its own what is durable.

Tests: use fakes, a real temporary SQLite database following the established
fixture pattern in tests/, and a recording fake IngestionService. Cover the
stored-sufficient path (no discovery), the stored-insufficient path (one
discovery), the repeat-query path (reuse, no second ingestion), each outage,
the auto_read=0 assertion, the clamp, id validation stripping an unknown id,
and that a casual conversational request performs zero retrieval and zero
ingestion.

--- WORKING AGREEMENT (identical for every worker in this phase) ---
Repo: ResearchRadar, Python 3.12, package root src/research_radar.
You are ONE of twelve parallel workers. Other workers are editing other files
in other worktrees at the same time.

READ FIRST, IN FULL: docs/PHASE_CONTRACTS.md in your worktree. It is the
binding interface contract for this phase. Implement exactly the module paths,
class names, method signatures, field names, and settings names it defines. Do
not rename or "improve" a contract name. If a contract is genuinely wrong,
escalate with `orca orchestration ask` instead of changing it unilaterally.

Your worktree is already on your own branch, based on
feat/personal-research-chat-memory.

Run tests and lint with the SHARED interpreter. Do NOT create a virtualenv and
do NOT pip install anything unless your task explicitly says so:
  cd "$PWD"   # your worktree root
  PYTHONPATH="$PWD/src" /Users/alvinluong/ResearchRadar/.venv/bin/python -m pytest -q
  /Users/alvinluong/ResearchRadar/.venv/bin/ruff check .

House style: ruff rules E,F,I,B,UP; line length 100; `from __future__ import
annotations` first in every module; docstrings on public classes and methods;
pytest asyncio_mode=auto (do NOT add @pytest.mark.asyncio).

Hard rules:
- Edit ONLY the files listed under OWNED FILES. Touching another worker's file
  causes an integration conflict and will be reverted.
- Never write a real credential into any file, test, fixture, log, or commit.
- Never push to main, never merge, never open a PR. Commit on your own branch.
- Tests must pass offline with no network access.
- Do not run blocking CPU/IO work directly on an asyncio event loop; wrap
  SQLite / filesystem / embedding / Pinecone calls with asyncio.to_thread.
- When finished, commit your work, then send exactly one worker_done with
  --outcome succeeded or --outcome failed and a 3-sentence summary.
